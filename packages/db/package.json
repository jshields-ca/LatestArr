{
  "name": "@latestarr/db",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "drizzle-orm": "^0.45.3"
  },
  "devDependencies": {
    "@latestarr/config": "workspace:*",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.20.4",
    "drizzle-kit": "^0.31.11",
    "eslint": "^10.10.0",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  }
}
